/**
 * Onboarding feature flags.
 *
 * Keep step components in the tree; flip these to re-enable flows later.
 */

/** AI Providers step (API keys / model choices) during first-run. */
export const ENABLE_PROVIDER_SETUP_STEP = false

/** "Migrate from Claude Code / Cursor / OpenCode?" cards on the complete step. */
export const ENABLE_MIGRATION_OFFERS = false
